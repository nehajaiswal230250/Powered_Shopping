{
  "name": "voice-powered-shopping",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "sh -c 'trap \"kill 0\" EXIT INT TERM; npm run dev --prefix server & npm run dev --prefix client'",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  }
}
